---
impact: minor
type: feat
area: setup
---

# Add pragmatic active-note folder setup

User-visible change:

- A single setup command creates new bindings quickly and can explicitly restore a unique imported exact-path marker identity.

Validation:

- `npm run test`
- `npm run build`
- `npm run lint`
- `npm run test:integration`

---
impact: minor
type: feat
area: reporting
---

# Add review navigation and resizable folder details

Show solid light/dark full-row attention colors and use shared captured availability checks to
identify potentially adoptable branches and leaves. Add a Needs review filter
and Previous/Next issue navigation over actual matches, including collapsed
branches. Pending recovery takes precedence across overlapping session changes.

Add a session-only accessible pane divider and reorganize expanded folder details
around relationships and actions. Preserve selection detail state and focus across
redraws, and annotate status export explanations when session changes affect the
results. The shared UI remains available in Obsidian and offline HTML. Adoption
previews, execution safeguards, and existing CSV columns remain unchanged.

Use compact rows with aligned leaf-quantity, descriptor, and evidence columns,
sticky headings, orange review states, and distinct selection and focus outlines. Allow normal selection and
copying of details text in both hosts.

Apply the verified equal-lightness row and hover palette with green healthy
bindings, blue optional actions, orange review states, and red conflicts. Keep
foreground indicator colors separate from row fills and use contrasting row
text in both themes. Obsidian theme selection overrides the system preference.
